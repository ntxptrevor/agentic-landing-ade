{
  "name": "mygmc-smartcar",
  "description": "myGMC / GM EV integration via the Smartcar API. Read battery, charge status, location, odometer, and tire pressure, and send lock/unlock and start/stop charge commands for a vehicle such as a 2026 GMC Sierra EV.",
  "version": "1.0.0",
  "author": {
    "name": "NTXP"
  },
  "mcpServers": {
    "mygmc-smartcar": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/src/index.js"],
      "env": {
        "SMARTCAR_CLIENT_ID": "${SMARTCAR_CLIENT_ID}",
        "SMARTCAR_CLIENT_SECRET": "${SMARTCAR_CLIENT_SECRET}",
        "SMARTCAR_REDIRECT_URI": "${SMARTCAR_REDIRECT_URI}",
        "SMARTCAR_MODE": "${SMARTCAR_MODE}",
        "SMARTCAR_UNIT_SYSTEM": "${SMARTCAR_UNIT_SYSTEM}"
      }
    }
  }
}
